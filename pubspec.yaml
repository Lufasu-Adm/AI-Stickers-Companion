name: ai_companion_app
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # HAPUS o3d, karena kita pakai stiker 2D
  # (TIDAK ADA O3D DI SINI)

  flutter_riverpod: ^2.5.1
  google_fonts: ^6.2.1
  dio: ^5.7.0
  just_audio: ^0.9.42
  flutter_animate: ^4.5.0
  
  # HAPUS model_viewer_plus, jika ada
  
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  
  assets:
    # HAPUS: - assets/3d/GLB/
    # GANTI: Mendaftarkan folder stiker Anda (sesuai folder image_eb58e0.png)
    - assets/stikcers/